import { FileUp } from "lucide-react";

export const studentContent = {
    menuItems: [
        { label: "Submit File", icon: <FileUp size={25} /> },
        { label: "Panel" },
        { label: "Adviser" },
    ]
}